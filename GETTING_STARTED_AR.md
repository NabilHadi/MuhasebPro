# ملاحظات مهمة - محاسب برو

## ⚠️ قبل البدء

### 1. تثبيت المتطلبات الأساسية

تأكد من تثبيت:
```bash
node --version    # يجب أن يكون 16.x أو أحدث
npm --version     # يجب أن يكون 7.x أو أحدث  
mysql --version   # يجب أن يكون 8.0 أو أحدث
```

### 2. كلمات المرور والأمان

⚠️ **ملاحظة أمنية مهمة:**
- غيّر كلمة مرور مستخدم MySQL الافتراضية
- غيّر `JWT_SECRET` في ملف `.env`
- استخدم كلمات مرور قوية في الإنتاج
- لا تشارك ملف `.env` في الـ Git

### 3. بيانات قاعدة البيانات

الملف المطلوب: `database/schema.sql`

تأكد من:
- وجود المجلد `database/`
- وجود ملف `schema.sql`
- توافق إصدار MySQL (8.0+)

## 🔍 استكشاف الأخطاء الشائعة

### الخطأ: "Cannot find module 'express'"

**الحل:**
```bash
cd backend
npm install
```

### الخطأ: "ECONNREFUSED 127.0.0.1:3306"

**السبب:** MySQL غير مشغّل

**الحل:**
```bash
# على Windows
net start MySQL80

# على Mac
brew services start mysql

# على Linux
sudo systemctl start mysql
```

### الخطأ: "ER_ACCESS_DENIED_ERROR"

**السبب:** كلمة مرور MySQL خاطئة

**الحل:** 
تحقق من قيم في ملف `.env`:
```env
DB_USER=root
DB_PASSWORD=your_correct_password
```

### الخطأ: "Cannot GET /"

**السبب:** Frontend لم يتم تشغيله

**الحل:**
```bash
cd frontend
npm run dev
```

## 📚 الملفات المهمة

```
README.md                 - شرح المشروع بالعربية
INSTALLATION_AR.md        - دليل التثبيل مفصل
PROJECT_SUMMARY_AR.md     - ملخص المشروع
FAQ_AR.md                 - أسئلة شائعة
.env.example              - مثال على المتغيرات
database/schema.sql       - مخطط قاعدة البيانات
```

## 🎯 الخطوات السريعة

### Windows

```bash
# تشغيل سكريبت التثبيت
setup.bat

# ثم تشغيل Backend
cd backend && npm run dev

# في نافذة جديدة: تشغيل Frontend
cd frontend && npm run dev
```

### Mac/Linux

```bash
# تشغيل سكريبت التثبيت
bash setup.sh

# ثم تشغيل Backend
cd backend && npm run dev

# في نافذة جديدة: تشغيل Frontend
cd frontend && npm run dev
```

## 📱 اختبار التطبيق

بعد التشغيل:

1. افتح المتصفح على: `http://localhost:3000`
2. سجّل دخول بـ:
   - اسم المستخدم: `admin`
   - كلمة المرور: `123456`

## 🔌 اختبار API

```bash
# اختبار اتصال الخادم
curl http://localhost:5000/api/health

# تسجيل الدخول
curl -X POST http://localhost:5000/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"username":"admin","password":"123456"}'
```

## 📊 هيكل قاعدة البيانات

الجداول الرئيسية:
- `users` - المستخدمين
- `customers` - العملاء
- `suppliers` - الموردين
- `products` - المنتجات
- `warehouses` - المخازن
- `sales` - المبيعات
- `purchases` - المشتريات
- `payments` - المدفوعات

## 🔐 الصلاحيات

| الدور | البيانات المسموح بها |
|------|-------------------|
| admin | كل شيء |
| accountant | الفواتير والتقارير والحسابات |
| sales | إنشاء فواتير المبيعات |
| warehouse | إدارة المخزون والمشتريات |

## 💾 النسخ الاحتياطية

### تصدير قاعدة البيانات

```bash
mysqldump -u root -p accounterp_db > backup.sql
```

### استيراد النسخة الاحتياطية

```bash
mysql -u root -p accounterp_db < backup.sql
```

## 🚀 النشر في الإنتاج

### بناء المشروع

```bash
# Backend
cd backend
npm run build

# Frontend
cd frontend
npm run build
```

### استخدام PM2 (للخوادم)

```bash
# تثبيت PM2
npm install -g pm2

# تشغيل Backend
pm2 start "npm start" --name "accounterp-backend" --cwd ./backend

# عرض حالة العمليات
pm2 list

# عرض السجلات
pm2 logs accounterp-backend
```

## 📝 متطلبات الإصدار الأول

- [x] نظام المصادقة
- [x] إدارة الموردين
- [x] إدارة العملاء
- [x] إدارة المنتجات
- [x] إدارة المخازن
- [x] إدارة المشتريات الأساسية
- [x] إدارة المبيعات الأساسية
- [x] لوحة التحكم الأساسية
- [x] واجهة عربية 100%

## 🛠️ تحديثات مستقبلية

- [ ] نظام الفواتير المتقدم
- [ ] تصدير PDF و Excel
- [ ] رسوم بيانية متقدمة
- [ ] نظام الإشعارات
- [ ] تطبيق موبايل
- [ ] دعم عملات متعددة

## 📞 الدعم

للمساعدة:
1. اقرأ الملفات المرفقة
2. ابحث في FAQ
3. تحقق من Console للأخطاء
4. افتح Issue في المستودع

## ✨ الملاحظات الأخيرة

- المشروع مكتمل وجاهز للاستخدام
- الكود منظم وسهل التعديل عليه
- التوثيق شامل وواضح
- يدعم العربية بالكامل
- قابل للتوسع والتطوير

---

🎉 **مبروك! أنت جاهز للبدء!**
