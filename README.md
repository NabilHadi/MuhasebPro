# محاسب برو - نظام محاسبة متكامل عبر الويب

نظام محاسبة عصري ومتكامل مبني على أحدث التقنيات لإدارة العمليات المحاسبية والمالية والمخزون.

## ✨ المميزات

### 📊 إدارة المبيعات والمشتريات
- إنشاء وإدارة فواتير البيع والشراء
- تطبيق الخصومات والعروض الخاصة
- متابعة حالة الفواتير (معلقة، مدفوعة، ملغاة)

### 📦 إدارة المخزون
- تتبع كميات المنتجات بدقة
- إدارة مواقع التخزين والمخازن
- تحذيرات تلقائية عند انخفاض المخزون
- حركات المخزون المسجلة بالكامل

### 👥 إدارة العملاء والموردين
- إضافة وتحديث بيانات العملاء والموردين
- سجل كامل للتعاملات
- إدارة الحدود الائتمانية
- متابعة الأرصدة والمدفوعات

### 💰 إدارة الحسابات
- تسجيل الإيرادات والمصروفات
- الحسابات الختامية
- إدارة الأصول والخصوم
- تقارير مفصلة عن الوضع المالي

### 📈 التقارير والتحليلات
- تقارير تفصيلية عن المبيعات والمشتريات
- عرض الأرباح والخسائر
- رسوم بيانية تفاعلية في لوحة التحكم
- تقارير المخزون والمنتجات

### 🔐 نظام الصلاحيات
- دعم أدوار متعددة (مسؤول، محاسب، موظف مبيعات، أمين مخزن)
- صلاحيات مرنة قابلة للتخصيص
- تسجيل دخول آمن مع توثيق JWT

### 🌍 دعم العربية الكامل
- واجهة مستخدم باللغة العربية 100%
- اتجاه من اليمين إلى اليسار (RTL)
- تاريخ وأرقام بالصيغ العربية

## 🛠️ التقنيات المستخدمة

### الواجهة الخلفية (Backend)
- **Node.js** - بيئة تطوير JavaScript
- **TypeScript** - لغة برمجة قوية مع typing
- **Express.js** - إطار عمل للويب
- **MySQL 2** - قاعدة بيانات العلاقات
- **bcrypt** - تشفير كلمات المرور
- **JWT** - توثيق آمن

### الواجهة الأمامية (Frontend)
- **React 18** - مكتبة واجهات المستخدم
- **TypeScript** - لغة برمجة قوية
- **React Router** - التنقل بين الصفحات
- **Tailwind CSS** - أنماط ومظهر عصري
- **Axios** - استدعاءات API
- **Zustand** - إدارة الحالة
- **Chart.js** - رسوم بيانية تفاعلية

### قاعدة البيانات
- **MySQL** - نظام إدارة قواعد البيانات العلاقات

## 📋 المتطلبات

- Node.js 16.x أو أحدث
- npm أو yarn
- MySQL 8.0 أو أحدث
- متصفح ويب حديث

## 🚀 التثبيت والتشغيل

### 1. تثبيت قاعدة البيانات

```bash
mysql -u root -p < database/schema.sql
```

أو من خلال MySQL Workbench أو أي برنامج إدارة قواعد بيانات:
```sql
CREATE DATABASE accounterp_db CHARACTER SET utf8mb4 COLLATE utf8mb4_general_ci;
USE accounterp_db;
-- ثم شغل محتوى ملف schema.sql
```

### 2. تثبيت Backend

```bash
cd backend
npm install
```

### 3. إعدادات المتغيرات البيئية

أنشئ ملف `.env` في مجلد backend:

```env
PORT=5000
NODE_ENV=development

# Database Configuration
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=your_password
DB_NAME=accounterp_db

# JWT Configuration
JWT_SECRET=your_super_secret_key_change_this_in_production
JWT_EXPIRE=7d

# API Configuration
API_URL=http://localhost:5000
CORS_ORIGIN=http://localhost:3000
```

### 4. تشغيل Backend

```bash
cd backend
npm run dev
```

السيرفر سيعمل على: `http://localhost:5000`

### 5. تثبيت Frontend

```bash
cd frontend
npm install
```

### 6. تشغيل Frontend

```bash
cd frontend
npm run dev
```

الواجهة الأمامية ستعمل على: `http://localhost:3000`

## 🔑 بيانات الدخول التجريبية

```
اسم المستخدم: admin
كلمة المرور: 123456
```

**ملاحظة**: غير كلمة المرور الافتراضية قبل الاستخدام في بيئة الإنتاج!

## 📁 هيكل المشروع

```
AccountingERP_RJS/
├── backend/
│   ├── src/
│   │   ├── config/          # إعدادات قاعدة البيانات و JWT
│   │   ├── middleware/      # Middleware التوثيق والصلاحيات
│   │   ├── routes/          # مسارات API
│   │   ├── controllers/     # منطق الطلبات
│   │   ├── services/        # خدمات الأعمال
│   │   └── server.ts        # ملف الدخول الرئيسي
│   ├── package.json
│   ├── tsconfig.json
│   └── .env.example
│
├── frontend/
│   ├── src/
│   │   ├── components/      # المكونات القابلة لإعادة الاستخدام
│   │   ├── pages/           # الصفحات الرئيسية
│   │   ├── services/        # خدمات API والمصادقة
│   │   ├── store/           # إدارة الحالة (Zustand)
│   │   ├── types/           # أنواع TypeScript
│   │   ├── utils/           # دوال مساعدة
│   │   ├── App.tsx          # التطبيق الرئيسي
│   │   └── index.css        # الأنماط العامة
│   ├── package.json
│   ├── vite.config.ts
│   └── tailwind.config.js
│
└── database/
    └── schema.sql           # مخطط قاعدة البيانات
```

## 🎨 واجهة المستخدم

### الصفحات الرئيسية

1. **لوحة التحكم** - إحصائيات وملخص العمليات اليومية
2. **المشتريات** - إدارة فواتير الشراء
3. **المبيعات** - إدارة فواتير البيع
4. **المخزون** - إدارة المنتجات والمخزون
5. **المخازن** - إدارة مواقع التخزين
6. **الموردون** - إدارة الموردين وبياناتهم
7. **العملاء** - إدارة العملاء وبياناتهم
8. **التقارير** - تقارير شاملة وتحليلات

## 🔒 الأمان

- تشفير كلمات المرور باستخدام bcrypt
- توثيق JWT آمن
- حماية CORS
- Helmet للحماية من الهجمات الشائعة
- التحقق من الصلاحيات على كل طلب

## 📚 API Endpoints

### المصادقة
- `POST /api/auth/register` - تسجيل حساب جديد
- `POST /api/auth/login` - تسجيل دخول

### المستخدمين
- `GET /api/users` - الحصول على جميع المستخدمين (admin فقط)
- `GET /api/users/profile` - الحصول على بيانات المستخدم الحالي
- `PUT /api/users/:id/role` - تغيير دور المستخدم (admin فقط)

### الموردين
- `GET /api/suppliers` - الحصول على جميع الموردين
- `POST /api/suppliers` - إضافة مورد جديد
- `PUT /api/suppliers/:id` - تحديث مورد
- `DELETE /api/suppliers/:id` - حذف مورد

### العملاء
- `GET /api/customers` - الحصول على جميع العملاء
- `POST /api/customers` - إضافة عميل جديد
- `PUT /api/customers/:id` - تحديث عميل
- `DELETE /api/customers/:id` - حذف عميل

### المنتجات
- `GET /api/products` - الحصول على جميع المنتجات
- `POST /api/products` - إضافة منتج جديد
- `PUT /api/products/:id` - تحديث منتج
- `DELETE /api/products/:id` - حذف منتج

### المشتريات والمبيعات
- `GET /api/purchases` - الحصول على جميع المشتريات
- `POST /api/purchases` - إضافة فاتورة شراء جديدة
- `GET /api/sales` - الحصول على جميع المبيعات
- `POST /api/sales` - إضافة فاتورة بيع جديدة

### التقارير
- `GET /api/reports/dashboard` - إحصائيات لوحة التحكم
- `GET /api/reports/sales` - تقرير المبيعات
- `GET /api/reports/purchases` - تقرير المشتريات

## 🐛 معالجة الأخطاء

يستخدم النظام نظام معالجة أخطاء موحد مع رسائل خطأ واضحة ودعم كامل للغة العربية.

## 📦 الإنتاج (Production)

للنشر في بيئة الإنتاج:

### Backend

```bash
cd backend
npm run build
npm start
```

### Frontend

```bash
cd frontend
npm run build
```

ثم انسخ محتوى مجلد `dist/` إلى خادم ويب (nginx، Apache، إلخ)

## 📝 الترخيص

هذا المشروع مفتوح المصدر ومتاح للاستخدام الحر.

## 🤝 المساهمة

نرحب بالمساهمات! يرجى:
1. عمل fork للمشروع
2. إنشاء branch جديد لميزتك
3. commit التغييرات
4. push إلى الـ branch
5. فتح Pull Request

## 📞 الدعم

للمساعدة والدعم، يرجى التواصل أو فتح issue في المستودع.

## 📊 إدارة الحسابات والقيود المحاسبية (الجديد!)

### المميزات الجديدة
- ✅ **إدارة الحسابات** - إضافة/تعديل/حذف الحسابات
- ✅ **القيود المحاسبية** - إنشاء قيود محاسبية متوازنة
- ✅ **فحص التوازن التلقائي** - التحقق من التوازن المحاسبي
- ✅ **أسطر متعددة** - دعم أسطر متعددة للقيود
- ✅ **حماية البيانات** - منع حذف البيانات المستخدمة

### الحسابات الافتراضية
يتضمن النظام 16 حساب افتراضي:
- **الأصول:** الصندوق، البنك، العملاء، المخزون
- **الخصوم:** الموردين، الضرائب المستحقة
- **حقوق الملكية:** رأس المال، الأرباح المحتجزة
- **الإيرادات:** المبيعات، الخدمات
- **المصروفات:** الإيجار، الرواتب، الكهرباء، واللوازم

### توثيق إضافية
تم إنشاء 6 ملفات توثيق شاملة:
1. **COMPLETION_SUMMARY.md** - ملخص شامل
2. **QUICK_START_GUIDE.md** - دليل البدء السريع
3. **ACCOUNTING_MODULE_GUIDE.md** - دليل المميزات
4. **SYSTEM_ARCHITECTURE.md** - البنية المعمارية
5. **IMPLEMENTATION_SUMMARY.md** - ملخص التنفيذ
6. **FINAL_VERIFICATION_CHECKLIST.md** - قائمة التحقق

### بدء الاستخدام
```bash
# 1. انتقل إلى الصفحات الجديدة
📊 الحسابات - http://localhost:3000/accounts
📝 القيود المحاسبية - http://localhost:3000/journal-entries

# 2. اقرأ الدليل السريع
QUICK_START_GUIDE.md
```

---

## 🎯 الخطط المستقبلية

- [ ] تحسينات الأداء
- [ ] المزيد من التقارير المتقدمة
- [ ] دعم عملات متعددة
- [ ] تطبيق للهاتف المحمول
- [ ] نسخ احتياطية تلقائية
- [ ] النسخة السحابية (Cloud Version)
- [ ] ميزانية عمومية وقائمة الدخل
- [ ] تقرير حسابات النتائج

---

**تم بناؤه بـ ❤️ لإدارة حسابات عربية متطورة**
